name: dashboard_asistencia
description: Dashboard de Asistencias SENA - Sistema en Tiempo Real
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP Client
  http: ^1.2.2

  # Icons
  cupertino_icons: ^1.0.2

  # Charts (versión estable compatible)
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
